# permissions for end users to view repos.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: repo-viewer-role
rules:
- apiGroups:
  - src.bwag.me
  resources:
  - repos
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - src.bwag.me
  resources:
  - repos/status
  verbs:
  - get
